import 'package:flutter/material.dart';

class AppTextTheme {
  static const TextTheme lightTextTheme = TextTheme(
    bodyLarge: TextStyle(color: Color(0xff121212)), // Large text on light bg
    bodyMedium: TextStyle(color: Color(0xff333333)), // Normal text
  );

  static const TextTheme darkTextTheme = TextTheme(
    bodyLarge: TextStyle(color: Color(0xff121212)), // Large text on light bg
    bodyMedium: TextStyle(color: Color(0xff333333)), // Normal text
  );
}
